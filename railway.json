{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --prefix custom-mcp-server && npm run build --prefix custom-mcp-server && npm install --prefix agent-backend && npm run build --prefix agent-backend"
  },
  "deploy": {
    "startCommand": "node agent-backend/dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
